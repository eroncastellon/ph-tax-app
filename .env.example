# ============================================
# Philippine Tax Guidance API - Environment Variables
# ============================================

# Node Environment
NODE_ENV=development

# API Server
PORT=3000
API_PORT=3000

# Database
DATABASE_URL="postgresql://taxapp:taxapp_secret@localhost:5432/ph_tax_guidance?schema=public"
POSTGRES_USER=taxapp
POSTGRES_PASSWORD=taxapp_secret
POSTGRES_DB=ph_tax_guidance
POSTGRES_PORT=5432

# JWT Authentication
# IMPORTANT: Generate a strong secret for production!
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=15m

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================
# Optional: External AI Service (for enhanced explanations)
# ============================================
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-...

# ============================================
# NOTES
# ============================================
# 1. Copy this file to .env and update values for your environment
# 2. NEVER commit .env to version control
# 3. Generate a strong JWT_SECRET for production
# 4. Update DATABASE_URL for production database
